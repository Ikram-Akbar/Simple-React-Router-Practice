import React from 'react';

const Friend = () => {
    return (
        <div>
            <h1>this page is belongs to a single page of a single friend</h1>
        </div>
    );
};

export default Friend;